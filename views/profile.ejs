<div class="container py-5"> 
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card shadow-lg rounded-4 text-center p-4 border-0">
        
        <!-- Back Icon (aligned left) -->
        <div class="text-start mb-3">
          <a href="/rideShare" class="text-decoration-none text-primary fs-5">
            <i class="fa-solid fa-arrow-left"></i> Back
          </a>
        </div>

        <h2 class="fw-bold text-primary mb-2">User Profile</h2>
        <p class="text-muted mb-4">Welcome back, <strong><%= user.username %></strong>!</p>

        <ul class="list-group list-group-flush text-start">
          <li class="list-group-item"><strong class="text-primary">👤 Username:</strong> <%= user.username %></li>
          <li class="list-group-item"><strong class="text-primary">📧 Email:</strong> <%= user.email %></li>
          <li class="list-group-item"><strong class="text-primary">📞 Phone:</strong> <%= user.number %></li>
          <li class="list-group-item"><strong class="text-primary">📅 Member Since:</strong> <%= user.date.toLocaleDateString() %></li>
        </ul>

        <a href="/logout" class="btn btn-outline-primary mt-4 px-4 rounded-pill fw-semibold">Logout</a>
      </div>
    </div>
  </div>
</div>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
